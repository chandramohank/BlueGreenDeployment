apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-demo
  annotations:
    kubernetes.io/ingress.class: ingress-nginx
spec:
  rules:
    http:
      paths:
      - backend:
          service:
            name: webapp-demo
            port:
              number: 80
        path: /
        pathType: Prefix
